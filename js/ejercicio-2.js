// Crea un vector con los números del 1 al 5. Luego elimina el número que está en la segunda posición y muestra el vector

// Inicializar vector
let numeros = [1, 2, 3, 4, 5];

numeros.splice(1, 1);

// Muestro el vector
console.log(numeros);