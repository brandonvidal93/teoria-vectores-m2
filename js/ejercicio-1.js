// Crear un vector vacío llamado "colores". Luego agrega 3 colores diferentes y recorre el vector.











// Inicializo el vector vacio
let colores = [];

// Agrego información
colores.push("naranja", "negro", "azul");

// Recorro el vector
for(let i = 0; i < colores.length; i++) {
  console.log(colores[i]);
}